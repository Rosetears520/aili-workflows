{
  "name": "rose-aili",
  "version": "0.1.11",
  "description": "AILI OpenCode workflow installer CLI",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/Rosetears520/aili-workflows"
  },
  "type": "module",
  "bin": {
    "rose-aili": "dist/cli.js"
  },
  "files": [
    "dist/",
    "agents/",
    ".agents/",
    "commands/",
    "manifests/",
    "templates/opencode-global-AGENTS.md",
    "scripts/install_opencode.sh",
    "README.md",
    "docs/opencode-setup.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json && node scripts/make-cli-executable.mjs",
    "prepare": "npm run build",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node --test tests/*.test.mjs"
  },
  "dependencies": {
    "jsonc-parser": "^3.3.1"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=20.19.0"
  }
}
