# Workflow Orchestration Source Register

Schema: `aili.workflow-orchestration-sources.v1`

## Authority boundary

[KNOWN] This register is the checked inventory and ownership index for OpenSpec change `complete-aili-workflow-orchestration`. It points to canonical owners; it does not replace their behavior, the accepted OpenSpec contract, generated-source rules, fresh verification, or final ROSE reconciliation. Machine-readable entries live in `docs/harness/fixtures/workflow-orchestration-fixtures.yaml` and are checked by `scripts/workflow_contract_check.py`.

[KNOWN] Every entry uses exactly one source class: `canonical source`, `generated/installed adapter`, `upstream reference`, `historical evidence`, or `out of scope`. Active DCP and pseudo Goal entries are Package 4/5 inventory targets, not Package 1 residual violations.

## Resolved canonical paths

| Concern | Canonical path | Owner | Package-1 disposition |
|---|---|---|---|
| Source register | `docs/harness/workflow-orchestration-source-register.md` | Package 1 | [KNOWN] Created as an index, not behavior authority. |
| Artifact, delta, convergence-link, and loop-envelope fields | `.agents/skills/aili-delivery-flow/references/artifact-contracts.md` | Package 1 shared scaffold; later behavior by Packages 2, 3, 5, 7 | [KNOWN] Reuses the existing artifact-contract owner. `CONT-005` remains the normative budget owner. |
| Finding/result fields | `.agents/skills/aili-delivery-flow/references/protocols/subagent-result.md` | Package 1 shared scaffold; Package 7 behavior | [KNOWN] Reuses the existing result protocol. |
| Worktree context protocol | `.agents/skills/aili-delivery-flow/references/protocols/worktree-context.md` | Package 6 | [KNOWN] Reserved as the single `WT-001` protocol path. Package 1 defines only references; cross-root behavior remains blocked until Package 6 creates and validates it. |
| Role overlay references | `.agents/skills/aili-delivery-flow/references/protocols/subagent-task-packet.md` and `.agents/skills/aili-delivery-flow/references/protocols/subagent-result.md` | Package 1 shared scaffold; Package 6 behavior | [KNOWN] Delegation references the worktree-owned context and does not duplicate it. |
| Review arbitration runtime artifact | `openspec/changes/<change-id>/review-arbitration.md` | Package 7 | [KNOWN] Exact change-local artifact path; no Package-1 artifact instance is created. |
| Graphify local-review reference | `.agents/skills/local-review-gate/references/graphify-local-review.md` | Package 9 | [KNOWN] Reserved under the existing local-review capability; Package 1 does not create, install, or run Graphify. |

## Inventory and package/file ownership

[KNOWN] The machine-readable `package_file_ownership` array in the fixture assigns exact edit files for Packages 1-11 and records Package 12 as verification-only. Shared files appear in more than one package only when later behavior/synchronization work must edit the same canonical owner serially; this is staged ownership, not permission for overlapping parallel edits. Goal checker/document surfaces are assigned serially to Packages 5, 10, and 11 according to behavior, synchronization, and aggregate-check ownership. `.gitignore` is inspection-only generated-boundary evidence because the accepted tasks assign no edit to it.

[KNOWN] Package 6 permission ownership includes exactly these role files: `agents/rose.md`, `agents/agent-evaluator.md`, `agents/ai-regression-scout.md`, `agents/browser-qa-runner.md`, `agents/code-reviewer.md`, `agents/code-scout.md`, `agents/convergence-reviewer.md`, `agents/debug-investigator.md`, `agents/doc-researcher.md`, `agents/e2e-artifact-runner.md`, `agents/implementer.md`, `agents/opensource-sanitizer.md`, `agents/plan-auditor.md`, `agents/pr-test-analyzer.md`, `agents/security-auditor.md`, `agents/silent-failure-reviewer.md`, `agents/spec-miner.md`, `agents/test-coverage-reviewer.md`, `agents/test-engineer.md`, `agents/web-performance-auditor.md`, and `agents/web-researcher.md`. This covers parent, research/read-only, implementation, test/debug, review/security/coverage/convergence, and specialized permission-bearing roles.

[KNOWN] The fixture declares every P2-P11 shared edit path as a serial overlap with its exact package sequence and reason. The checker derives overlaps independently and fails on missing, extra, or mismatched declarations. Wave A Package 4, Package 6, and Package 8 edit sets must remain pairwise disjoint.

| Area | Current source class and exact surfaces | Package/file owner |
|---|---|---|
| DCP runtime | [KNOWN] `src/cli.ts`, `src/config.ts`, `src/installer.ts`, `src/doctor.ts` are active canonical source inventory targets. | Package 4 owns those exact files. |
| DCP docs/manifests/tests | [KNOWN] `README.md`, `docs/opencode-setup.md`, `templates/opencode-global-AGENTS.md`, `manifests/rose-aili.components.json`, `tests/rose-aili.test.mjs`, `agents/rose.md`, `.agents/skills/rose-memory/SKILL.md` contain active DCP behavior or references. | Package 4 owns runtime tests; Package 10 owns synchronized docs/templates/manifests/active skill references. |
| Neutral BUILD | [KNOWN] `commands/build.md`, `.agents/skills/aili-delivery-flow/SKILL.md`, `.agents/skills/aili-delivery-flow/references/build-execution-loop.md`, `backend-routing.md`, `artifact-contracts.md`, `lifecycle.md`, `docs/harness/aili-harness-contract.md`, `docs/harness/command-lifecycle.md`, both command/subagent fixtures, `scripts/harness_fixture_check.py`, and `scripts/delegation_protocols_check.py` are active inventory targets. | Package 5 owns neutral BUILD behavior; Package 10 owns synchronized docs/manifests; Package 11 aggregates tests. |
| Classifier and grilling | [KNOWN] `agents/rose.md`, four `commands/*.md` delivery entries, `.agents/skills/aili-delivery-flow/SKILL.md` and its lifecycle/backend/questionnaire/artifact references, `.agents/skills/requirements-grilling/SKILL.md`, `.agents/skills/using-agent-skills/SKILL.md`, `docs/harness/command-lifecycle.md`, fixtures/checker, and component manifest are canonical sources. | Package 2 owns classifier/grilling behavior; Packages 10/11 own synchronization/aggregation. |
| Continuity, memory, handoff | [KNOWN] `templates/opencode-global-AGENTS.md`, `agents/rose.md`, delivery lifecycle/backend/artifact references, `.agents/skills/rose-memory/`, `.agents/skills/session-handoff/SKILL.md`, `workflow.components.yaml`, component manifest, setup docs, fixtures, and checker are canonical sources. | Package 3 owns behavior; Packages 10/11 own synchronization/aggregation. |
| Review and convergence | [KNOWN] `.agents/skills/review-pipeline/SKILL.md`, `.agents/skills/local-review-gate/SKILL.md`, review/test/security/coverage/convergence agents, review-repair and result/report protocols, `commands/local-review.md`, fixtures, and harness checkers are canonical sources. | Package 7 owns review/convergence behavior and Package 11 aggregates checks. |
| Cross-root and role permissions | [KNOWN] `agents/rose.md`; all research/read-only, implementer, test/debug, review/security/coverage/convergence, browser/E2E, sanitizer, plan/spec, performance, and other specialized role files under `agents/`; task/result protocols; parallel dispatch skill; implementation package references; backend routing; and delegation checker are canonical sources. | Package 6 owns the exact worktree-context protocol, every permission-bearing role overlay, probe, and permission fixtures. Package 7 later serially normalizes result behavior for its declared review roles. |
| CodeGraph | [KNOWN] `src/cli.ts`, `src/installer.ts`, `src/doctor.ts`, Node tests, `templates/opencode-global-AGENTS.md`, `agents/rose.md`, `agents/code-scout.md`, relevant evidence/AGENTS/review skills, `README.md`, and setup docs are canonical source inventory targets. `.gitignore` is read only to prove the generated boundary. | Package 9 owns policy behavior; Package 10 owns synchronization; no package is assigned a `.gitignore` edit. |
| Graphify | [KNOWN] Current change design/specs are accepted contract; private `ideas/` content is unavailable historical input and is not a required repository source; `openspec/changes/integrate-codegraph-graphify-workflow/` is forbidden historical scope. | Package 9 owns only the new reference/checker/fixture/launcher surfaces named by the accepted change. |
| Upstream provenance | [KNOWN] Canonical skill provenance, `.agents/skills/requirements-grilling/references/`, `.agents/skills/local-review-gate/references/upstream-provenance.md`, `README.md`, package metadata, and component manifest are current evidence. | Package 8 owns inert pinned closures, notices, licenses, manifest, checker, fixtures, and thin adapters; Package 10 owns distribution synchronization. |
| Docs/manifests/fixtures/tests/generated | [KNOWN] `docs/`, `workflow.components.yaml`, `manifests/rose-aili.components.json`, `docs/harness/fixtures/`, `scripts/harness_fixture_check.py`, `scripts/delegation_protocols_check.py`, and `tests/rose-aili.test.mjs` are canonical sources. Root `AGENTS.md` alone is the generated project-local adapter; `templates/AGENTS.md` and `scripts/agents_md.py` remain its canonical source/generator. `dist/`, `.opencode/`, installed OpenCode/shared-skill targets, `.codegraph/`, `memory/`, and `node_modules/` are generated/installed/runtime adapters or outputs. | Packages 2-11 own exact behavior-specific files; Package 10 owns synchronization; Package 11 owns aggregate traceability; Package 12 verifies only. |

## Generated and historical boundaries

- [KNOWN] `.opencode/commands/opsx-*.md` and `.opencode/skills/openspec-*` are ignored generated/direct OpenSpec adapters. Phase I does not hand-edit, suppress, wrap, route to, recommend, or treat their direct output as AILI evidence.
- [KNOWN] Root `AGENTS.md` is generated from `templates/AGENTS.md`; source changes go to the template/generator contract rather than a duplicated managed mapping.
- [KNOWN] `.agents/skills/aili-delivery-flow/references/build-execution-loop.md` is the neutral BUILD authority. The retired `build-goal-mode.md` file is historical migration evidence only and must never appear in an active authority reference.
- [KNOWN] `dist/` is generated from TypeScript, and installed `$HOME/.config/opencode` / `$HOME/.agents` targets are downstream adapters rather than repository source.
- [KNOWN] `archive/` is checked-in historical evidence. Private `ideas/` content is unavailable historical input, is not inventoried or scanned by the gate, and cannot affect the result whether locally present or absent.
- [KNOWN] `openspec/changes/integrate-codegraph-graphify-workflow/` is forbidden edit/archive/apply scope. The rule is `no Git-status/diff unchanged proof`: Git status or diff cannot prove its ignored bytes unchanged, and no such byte-level claim is made.

## Rejected machinery and out-of-scope concepts

[KNOWN] Package 1 and later packages must not assume or create `baseline-manifest.json`, content digests, revision/nonce/receipt approval, a worktree registry or maintenance plane, P1-P4 permission profiles, independent target sessions, native/global SQLite authority, native `/goal`, background scheduling/listening/daemon/queue/hook runtime, or a Part-II native bridge.

[KNOWN] C-SCAFFOLD derives its rejected-machinery scan from every existing P2-P11 package-owned authority file. The final residual profile instead derives roots independently from checked-in registry/manifests, the source tree, and a checker-owned package allowlist; fixture package ownership and fixture source rows cannot select or shrink those roots. Legitimate active-documentation matches require exact path, rule, line, column, evidence, and full-line context entries. The checker uses no generic polarity heuristic. Exact checker/fixture/mutation-test control files are excluded by checker-owned constants, and every other unallowlisted semantic match is an active violation.

## Checker profile contracts

- [KNOWN] `scaffold` reads `profiles.scaffold.contract_state`: `pass` requires empty `blocked`/`unverified`; `blocked` requires a reason; `Unverified` requires a named gap. Valid blocked or `Unverified` state exits `3`; definite schema, path, ownership, or active-semantic violations exit `5`.
- [KNOWN] A ready `generated-adapter-boundary` fixture must be version `1.0`, target this change, provide the exact four route-to-command bindings, the exact generated roots and eight current adapter cases, the no-routing/no-evidence claim boundary, and cases covering every expected classification. The checker independently reads the four AILI commands, eight generated files, `.gitignore`, and this register; missing/mismatched repository evidence is a violation and unreadable evidence is `Unverified`.
- [KNOWN] A ready `residual` fixture must provide version/change/proof scope, the checker's exact semantic rules, exact occurrence allowlists, repository-derived matches/classifications, and cases covering active violation, negative documentation, historical evidence, generated adapter, and out-of-scope semantics. Roots come from the checked-in registry/manifests/source-tree/package allowlist, not fixture ownership, `residual.scan_roots`, or a selectable fixture source subset. The checker scans each resolved file deterministically; an active match or mismatch is a violation and unreadable evidence is `Unverified`.

## Package 12 aggregate test registry and audit boundary

[KNOWN] The Package-12 command registry includes both `npm run build && node --test tests/*.test.mjs` and `python -m unittest discover -s tests -p '*.py'`; `npm test` runs both so every Python test module, including Graphify and upstream stdlib tests, cannot be omitted by the aggregate command.

[KNOWN] Every test-evidence alias cites a concrete repository source path and an observed fixture case ID or executable test ID before entering the catalog. Coordinated mutation of an alias and its requirement reference still fails when the cited concrete source/ID does not exist.

[KNOWN] P11 generates the canonical nine-field template for all 74 checklist tasks with `Partial` status, unresolved ROSE-owned disposition, and an explicit non-final freshness marker. It stays separate and never claims final completion. `--final-closure` reads a separate repository-relative ROSE-owned task-audit JSON supplied only after reconciliation; it rejects the P11 fixture/template path, any non-exact or nonpassing row, stale or non-task-specific evidence, unresolved findings/dispositions, and unresolved `UV-001` read-only/nested-task runtime enforcement. The checker does not create that final artifact.

[UNVERIFIED] `UV-001`: Package 12 policy requires read-only `edit: deny`/`task: deny` overlays, but declarative agent, packet, and fixture fields do not prove runtime enforcement. Preserve the policy and this gap until an executable runtime permission probe demonstrates the effective final overlay.
- [KNOWN] Every inventory, canonical, package ownership, overlap, protocol-contract, and scan-exclusion path uses normalized repository-relative POSIX syntax. Absolute paths, `.`/`..`, backslashes, aliases, and unapproved placeholders/anchors are violations; only canonical review arbitration may use the `<change-id>` segment and only the loop envelope may use `#shared-loop-envelope`.
- [KNOWN] Existing paths are resolved before inspection, and a symlink or alias that resolves outside the project is rejected. Canonical result fields are parsed in declared order with unique fields and exactly one FINDINGS/NO FINDINGS template pair; duplicate, reordered, or ambiguous blocks are rejected.
- [KNOWN] C-SCAFFOLD executes all seven declared regression probes and includes their individual status/evidence in `regression_probes`; declared probe metadata alone cannot satisfy the scaffold.

## Repository evidence and reuse gate

[KNOWN] `.agents/skills/repo-evidence-first/SKILL.md` owns the executable discipline: inspect canonical code/docs/specs/tests/config/registries/manifests/templates/generators and peer patterns; select the existing owner; reuse before creating; return from generated output to its source/generator; stop and ask on material ambiguity; run fresh verification; and re-read edited files before a completion claim. The scaffold fixture checks these markers so a duplicate config/registry/template/schema/generator mapping cannot satisfy Package 1 silently.
