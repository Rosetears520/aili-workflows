# Workflow Orchestration Source Register

Schema: `aili.workflow-orchestration-sources.v1`

## Authority boundary

[KNOWN] This register is the checked inventory and ownership index for OpenSpec change `complete-aili-workflow-orchestration`. It points to canonical owners; it does not replace their behavior, the accepted OpenSpec contract, generated-source rules, fresh verification, or final ROSE reconciliation. Machine-readable entries live in `docs/harness/fixtures/workflow-orchestration-fixtures.yaml` and are checked by `scripts/workflow_contract_check.py`.

[KNOWN] Every entry uses exactly one source class: `canonical source`, `generated/installed adapter`, `upstream reference`, `historical evidence`, or `out of scope`. Active DCP and pseudo Goal entries are Package 4/5 inventory targets, not Package 1 residual violations.

## Later-authority overlay

[KNOWN|USER] OpenSpec change `simplify-aili-workflow-harness` is the later accepted authority for general harness-efficiency behavior: one primary loop plus at most one concrete auxiliary capability, safe-local autonomy, event/dependency-directed hydration, one claim-matched verification owner, single-use Task contexts, and retirement of `using-agent-skills`, `repo-evidence-first`, and `verification-before-completion`. Source: `openspec/changes/simplify-aili-workflow-harness/{proposal.md,design.md,test-plan.md}` and current-session acceptance `m0149` / BUILD intent `m0156`.

[FRAME] This register and its umbrella fixture remain current only for `complete-aili-workflow-orchestration` identities, historical package/task traceability, generated-adapter boundaries, CodeGraph/Graphify safety, exact 20-agent inventory, A33 admission/operation semantics, and unresolved A33 runtime evidence. Their former default fan-out, all-artifact hydration, repeated grilling/stress/review, per-slice/full-suite verification, old-Task continuation, and retired-skill ownership are superseded or migrated to the later canonical owners. Nothing in this overlay completes, rewrites, or upgrades the umbrella's unrelated A33 runtime/`Unverified` evidence.

## Resolved canonical paths

| Concern | Canonical path | Owner | Package-1 disposition |
|---|---|---|---|
| Source register | `docs/harness/workflow-orchestration-source-register.md` | Package 1 | [KNOWN] Maintained as an index, not behavior authority. |
| Artifact, delta, convergence-link, and loop-envelope fields | `.agents/skills/aili-delivery-flow/references/artifact-contracts.md` | Package 5, then Package 3 | [KNOWN] Package 1 inventories this existing owner only. `CONT-005` remains the normative budget owner. |
| Finding/result fields | `.agents/skills/aili-delivery-flow/references/protocols/subagent-result.md` | Packages 6 and 7 | [KNOWN] Package 1 inventories the existing compact result owner and does not edit its behavior. |
| Worktree context protocol | `.agents/skills/aili-delivery-flow/references/protocols/worktree-context.md` | Package 6 | [KNOWN] Existing single `WT-001` protocol path. Package 1 defines fixture/checker fields only; Package 6 reconciles and validates current A33 behavior. |
| Task/result and role references | `.agents/skills/aili-delivery-flow/references/protocols/subagent-task-packet.md` and `.agents/skills/aili-delivery-flow/references/protocols/subagent-result.md` | Package 6, then Package 7 result normalization | [KNOWN] Package 1 defines fixture/checker fields only; later packages own behavior. |
| Review arbitration runtime artifact | `openspec/changes/<change-id>/review-arbitration.md` | Package 7 | [KNOWN] Exact change-local artifact path; no Package-1 artifact instance is created. |
| Graphify reference | `README.md` | Package 9 | Graphify remains a separately approved orientation capability; this package does not create, install, or run it. |

## Inventory and package/file ownership

[KNOWN] The machine-readable `package_file_ownership` array assigns Package 1 exactly three edit owners: `scripts/workflow_contract_check.py`, `docs/harness/fixtures/workflow-orchestration-fixtures.yaml`, and this register. The prior seven-file Package-1 row is stale; artifact/task/result/evidence skill files are inspection-only for this package and remain assigned to their later behavior owners. Packages 2-11 retain exact edit rows and Package 12 is verification-only. Shared later files are serial staged ownership, not permission for overlapping parallel edits. `.gitignore` is inspection-only evidence because the accepted tasks assign no edit to it.

[KNOWN] The canonical inventory is exactly 20 Markdown files: primary `agents/rose.md` plus 19 managed subagents—`agent-evaluator`, `ai-regression-scout`, `browser-qa-runner`, `code-reviewer`, `code-scout`, `convergence-reviewer`, `doc-researcher`, `e2e-artifact-runner`, `implementer`, `opensource-sanitizer`, `plan-auditor`, `pr-test-analyzer`, `security-auditor`, `silent-failure-reviewer`, `spec-miner`, `test-coverage-reviewer`, `test-engineer`, `web-performance-auditor`, and `web-researcher`. Package 6 owns role behavior and installed equality. All 19 managed files currently declare `external_directory: deny`; ROSE remains distinct with per-operation ask. Built-in `explore`/`general` are excluded, and `agents/debug-investigator.md` is historical/deleted.

## Change-specific task and acceptance oracle

[KNOWN] The fixture and checker preserve the exact ordered 74 task IDs for this umbrella only. The checker derives `checked_task_ids`, `checked`, and `unchecked` fresh from the current `tasks.md` checklist marks on every run; mutable checkbox state is not fixture-bound. Generic lifecycle/convergence behavior must derive package identities and rows from the active accepted contract and MUST NOT impose 12 packages or 74 rows on another change.

[KNOWN|USER] A43/`DEF-E12-ACCEPT-DYNAMIC-TASK-STATE-ORACLE` accepts current item 44 without checking an implementation task. Item 44 carries item 43 forward and adds DEF-E12 dynamic task-state-oracle acceptance. A41/item 43 and its starting snapshot of 31 checked and 43 unchecked are historical/stale, not current acceptance metadata or a current invariant. A32/item 41, pre-A33 completion/task-audit evidence, the A33-era 32/42 count, OQ-008/item 42, and A30 runtime evidence are also historical or stale for current A33 readiness.

[KNOWN] The fixture declares every P2-P11 shared edit path as a serial overlap with its exact package sequence and reason. The checker derives overlaps independently and fails on missing, extra, or mismatched declarations. For shared delivery references, `lifecycle.md` is P2 → P5 → P3 (routing, then neutral BUILD, then continuity/memory behavior), and `artifact-contracts.md` is P5 → P3 (neutral BUILD/final-gate synchronization before continuity/memory behavior). Wave A Package 4, Package 6, and Package 8 edit sets must remain pairwise disjoint.

| Area | Current source class and exact surfaces | Package/file owner |
|---|---|---|
| DCP runtime | [KNOWN] `src/cli.ts`, `src/config.ts`, `src/installer.ts`, `src/doctor.ts` are active canonical source inventory targets. | Package 4 owns those exact files. |
| DCP docs/manifests/tests | [KNOWN] `README.md`, `docs/opencode-setup.md`, `templates/opencode-global-AGENTS.md`, `manifests/rose-aili.components.json`, `tests/rose-aili.test.mjs`, and `agents/rose.md` contain active DCP behavior or references. | Package 4 owns runtime tests; Package 10 owns synchronized docs/templates/manifests/active skill references. |
| Neutral BUILD | [KNOWN] `commands/build.md`, `.agents/skills/aili-delivery-flow/SKILL.md`, `.agents/skills/aili-delivery-flow/references/build-execution-loop.md`, `backend-routing.md`, `artifact-contracts.md`, `lifecycle.md`, `test-document-policy.md`, `docs/harness/aili-harness-contract.md`, `docs/harness/command-lifecycle.md`, both command/subagent fixtures, `scripts/harness_fixture_check.py`, and `scripts/delegation_protocols_check.py` are active inventory targets. | Package 2 first owns fail-closed acceptance/research semantics in `test-document-policy.md`; Package 5 then owns generic BUILD/final-gate synchronization and neutral BUILD behavior; Package 10 owns synchronized docs/manifests; Package 11 aggregates tests. |
| Classifier and grilling | [KNOWN] `agents/rose.md`, four `commands/*.md` delivery entries, `.agents/skills/aili-delivery-flow/SKILL.md` and its lifecycle/backend/questionnaire/test-document-policy/artifact references, `.agents/skills/requirements-grilling/SKILL.md`, `docs/harness/command-lifecycle.md`, fixtures/checker, and component manifest are canonical sources. [FRAME] `using-agent-skills` is retired; narrow capability selection is owned by ROSE/AILI routing. | Historical Package 2/5/10/11 ownership is preserved as umbrella traceability; `simplify-aili-workflow-harness` is later authority for general routing/grilling efficiency. |
| Continuity, memory, handoff | `templates/opencode-global-AGENTS.md`, `agents/rose.md`, delivery lifecycle/backend/artifact references, `core/governance/mempalace.md`, `core/commands/handoff.md`, `workflow.components.yaml`, component manifest, setup docs, fixtures, and checker are canonical sources. | Package 3 owns behavior; Packages 10/11 own synchronization/aggregation. |
| Review and convergence | `.agents/skills/review-pipeline/SKILL.md`, `core/commands/local-review.md`, review/test/security/coverage/convergence agents, review-repair and result/report protocols, generated `commands/local-review.md`, fixtures, and harness checkers are canonical sources. | Package 7 owns review/convergence behavior and Package 11 aggregates checks. |
| Cross-root and role permissions | [KNOWN] Existing Git-workflow skill; `WT-001`; compact task/result protocols; all 20 Agent files; delegation checker; existing permission probe/fixture/test; parallel dispatch; implementation-package and backend references are canonical sources. | Package 6 owns A33 behavior, exact role profiles, installed equality, checker/probe modes, and permission fixtures. Package 7 later owns convergence/result behavior; Package 11 aggregates. Package 1 inventories only. |
| CodeGraph | [KNOWN] `src/cli.ts`, `src/installer.ts`, `src/doctor.ts`, Node tests, `templates/opencode-global-AGENTS.md`, `agents/rose.md`, `agents/code-scout.md`, `core/commands/agents-md.md`, relevant AGENTS/review skills, `README.md`, and setup docs are canonical source inventory targets. [FRAME] `repo-evidence-first` is retired; its repository-evidence invariant is migrated to global Evidence Before Edits plus ROSE. `.gitignore` is read only to prove the generated boundary. | Package 10 owns the canonical `agents-md` Command source; historical Package 9/10 traceability does not transfer its current ownership. The later change owns the retired-skill migration without changing CodeGraph safety. |
| Graphify | [KNOWN] Current change design/specs are accepted contract; private `ideas/` content is unavailable historical input and is not a required repository source; `openspec/changes/integrate-codegraph-graphify-workflow/` is forbidden historical scope. | Package 9 owns only the new reference/checker/fixture/launcher surfaces named by the accepted change. |
| Upstream provenance | Canonical skill provenance, `.agents/skills/requirements-grilling/references/`, `README.md`, package metadata, and component manifest are current evidence. | Package 8 owns inert pinned closures, notices, licenses, checker, fixtures, and thin adapters; Package 10 owns the distribution catalog and synchronization. |
| Docs/manifests/fixtures/tests/generated/package | [KNOWN] `docs/`, both AGENTS templates, `workflow.components.yaml`, component manifest, `package.json`, fixtures, existing harness/delegation/workflow checkers, and tests are canonical sources. The ten canonical Commands are `core/commands/{ideate,define,build,ship,local-review,handoff,agents-md,harness-audit,retro,security-review}.md`; their `commands/` projections are generated runtime adapters. Root `AGENTS.md` alone is the generated project-local adapter; `templates/AGENTS.md` and `scripts/agents_md.py` remain its canonical source/generator. `dist/`, `.opencode/`, installed targets, `.codegraph/`, `memory/`, and `node_modules/` are generated/installed/runtime outputs. | Package 10 owns synchronization/package policy; Package 11 aggregates checks; Package 12 verifies only. Package 1 edits only its three owners. |

## Generated and historical boundaries

- [KNOWN] `.opencode/commands/opsx-*.md` and `.opencode/skills/openspec-*` are ignored generated/direct OpenSpec adapters. Phase I does not hand-edit, suppress, wrap, route to, recommend, or treat their direct output as AILI evidence.
- [KNOWN] Root `AGENTS.md` is generated from `templates/AGENTS.md`; source changes go to the template/generator contract rather than a duplicated managed mapping.
- [KNOWN] `.agents/skills/aili-delivery-flow/references/build-execution-loop.md` is the neutral BUILD authority. The retired `build-goal-mode.md` file is historical migration evidence only and must never appear in an active authority reference.
- [KNOWN] `dist/` is generated from TypeScript, and installed `$HOME/.config/opencode` / `$HOME/.agents` targets are downstream adapters rather than repository source.
- [KNOWN] `archive/` is checked-in historical evidence. Private `ideas/` content is unavailable historical input, is not inventoried or scanned by the gate, and cannot affect the result whether locally present or absent.
- [KNOWN] `openspec/changes/integrate-codegraph-graphify-workflow/` is forbidden edit/archive/apply scope. The rule is `no Git-status/diff unchanged proof`: Git status or diff cannot prove its ignored bytes unchanged, and no such byte-level claim is made.
- [KNOWN] A30/A31 external-read protocols, probes, and runtime results are historical-mode evidence only. Current A33 behavior must be implemented through the existing Package-6 Git/WT/task/result/role/checker/probe/fixture owners; Package 1 does not rewrite those protocols.

## Rejected machinery and out-of-scope concepts

[KNOWN] `a33_scaffold` is definition-only. It discriminates historical `a30-a31-external-read` from current `a33-attached-shared-trust-domain`; requires exact nonempty `repo_key`/`worktree_key` matching `^[A-Za-z0-9][A-Za-z0-9._-]{0,63}$` on PREPARE pending operations and every operation/approval; and defines the exact no-digest 17-field `A33Identity`. Host/source are populated, ADD target is absent→populated, and REMOVE target is populated→absent. Absent/inapplicable record data is JSON `null`, never omission or a string sentinel, except observed REMOVE approval `trusted_code_risk:not_applicable`.

[KNOWN] The same scaffold fixes target-rule re-read at operation/dispatch, narrow-only rules, same-level conflict block, one-repository lanes, and owning-target artifacts. Operation class is exactly `driver_fixture|real`; ADD requires accepted trusted-code risk. Branch mode is `existing|create`, source reflog policy is `enabled|disabled`, existing creates no ref/reflog, create always creates its exact ref and creates its exact reflog only when enabled, and remove preserves ref/reflog. Common-dir identity and all unrelated entries/refs/reflogs/config/hooks/worktree/prunable state remain unchanged; only the declared admin entry/membership and bound create-mode ref/reflog transaction are allowed. Behavior implementation remains with later packages.

[KNOWN] Package 1 and later packages must not assume or create `baseline-manifest.json`, content digests, revision/nonce/receipt approval, an A33 helper/registry/manifest/public command/dependency/daemon/maintenance plane/host selector/cleanup manager, P1-P4 permission profiles, independent target sessions, native/global SQLite authority, native `/goal`, background runtime, or a Part-II bridge. Existing owners are reused; no parallel mapping is created.

[KNOWN] Scaffold/residual rejection inventory also covers: generic fixed 12/74 lifecycle assumptions outside this umbrella; any managed-subagent external ask/allow; package inclusion of `.worktrees`, visible `worktrees`, or historical `.tmp/worktrees`; A30 evidence presented as current A33 proof; hard-isolation/sandbox claims; stale A32/OQ/item/task-count readiness; and unqualified Package-12 broad-review authority. Current `package.json` excludes all three worktree roots and retains the existing single CLI/dependency inventory. Later owners are explicitly flagged: P5/P6/P11 for A33 operation/runtime, P6/P10/P11 for role/install equality, P7/P11 for convergence/staleness, P10 for docs/templates/package synchronization, and P12 for the minimal BUILD completion check.

[KNOWN] C-SCAFFOLD derives its rejected-machinery scan from every existing P2-P11 package-owned authority file. The final residual profile instead derives roots independently from checked-in registry/manifests, the source tree, and a checker-owned package allowlist; fixture package ownership and fixture source rows cannot select or shrink those roots. Legitimate active-documentation matches require exact path, rule, line, column, evidence, and full-line context entries. The checker uses no generic polarity heuristic. Exact checker/fixture/mutation-test control files are excluded by checker-owned constants, and every other unallowlisted semantic match is an active violation.

## Checker profile contracts

- [KNOWN] `scaffold` reads `profiles.scaffold.contract_state`: `pass` requires empty `blocked`/`unverified`; `blocked` requires a reason; `Unverified` requires a named gap. Valid blocked or `Unverified` state exits `3`; definite schema, path, ownership, or active-semantic violations exit `5`.
- [KNOWN] A ready `generated-adapter-boundary` fixture must be version `1.0`, target this change, provide the exact four route-to-command bindings, the exact generated roots and eight current adapter cases, the no-routing/no-evidence claim boundary, and cases covering every expected classification. The checker independently reads the four AILI commands, eight generated files, `.gitignore`, and this register; missing/mismatched repository evidence is a violation and unreadable evidence is `Unverified`.
- [KNOWN] A ready `residual` fixture must provide version/change/proof scope, the checker's exact semantic rules, exact occurrence allowlists, repository-derived matches/classifications, and cases covering active violation, negative documentation, historical evidence, generated adapter, and out-of-scope semantics. Roots come from the checked-in registry/manifests/source-tree/package allowlist, not fixture ownership, `residual.scan_roots`, or a selectable fixture source subset. The checker scans each resolved file deterministically; an active match or mismatch is a violation and unreadable evidence is `Unverified`.
- [KNOWN] Usage errors exit `2`; passing profiles exit `0`; valid blocked/`Unverified` results exit `3`; contract violations exit `5`. The fixture documents the preserved JSON field sets: scaffold retains `schema_version,profile,status,sources,ownership,generated,traceability,regression_probes,blocked,unverified,errors` and adds `task_oracle,agent_inventory,a33_scaffold,a33_rejections`; generated-adapter retains its route/case/claim fields; residual retains `matches,classifications`; all retain `blocked,unverified,errors`.

## Later-owner and Package-12 boundary

[KNOWN] Existing command and aggregate traceability catalogs remain inventory for Packages 10/11. Under accepted item 44, which carries item 43 forward plus DEF-E12, they are selectable by changed scope/risk, not an automatic per-package or broad Package-12 matrix.

[KNOWN] Every test-evidence alias cites a concrete repository source path and an observed fixture case ID or executable test ID before entering the catalog. Coordinated mutation of an alias and its requirement reference still fails when the cited concrete source/ID does not exist.

[KNOWN] P11's existing nine-field task template remains non-final inventory and must be reconciled by its later owner. Package 12 is now only the minimal changed-scope BUILD completion check: ROSE runs the proactive delegation scan, inspects diff/task links, selects the smallest targeted verification, and dispatches trigger-eligible specialist contexts. Default concurrency is two but is not a hard cap; larger bounded fan-out requires independent non-overlapping contexts, concrete benefit, suitable owners, and an explicit join plan. At most one targeted repair/recheck remains. Broad integration/release review belongs only to later fresh SHIP intent. The checker creates no final audit artifact.

[UNVERIFIED] UV-006/UV-007 remain later runtime evidence gaps. Static Package-1 inventory proves neither effective installed profiles nor hard repository/process/network isolation; fail-closed Package-6/11 behavior must preserve those limits.
- [KNOWN] Every inventory, canonical, package ownership, overlap, protocol-contract, and scan-exclusion path uses normalized repository-relative POSIX syntax. Absolute paths, `.`/`..`, backslashes, aliases, and unapproved placeholders/anchors are violations; only canonical review arbitration may use the `<change-id>` segment and only the loop envelope may use `#shared-loop-envelope`.
- [KNOWN] Existing paths are resolved before inspection, and a symlink or alias that resolves outside the project is rejected. The compact canonical result block has ordered unique `status,evidence,blockers,confidence` fields; duplicate, reordered, or ambiguous blocks are rejected.
- [KNOWN] C-SCAFFOLD executes all seven declared regression probes and includes their individual status/evidence in `regression_probes`; declared probe metadata alone cannot satisfy the scaffold.

## Repository evidence and reuse gate

[FRAME] `repo-evidence-first` is retired. `templates/opencode-global-AGENTS.md` now owns the executable discipline: inspect relevant source/owner/tests/constraints, reuse an existing shared config/registry/manifest/template/schema/generator, return from generated output to its source/generator, stop only on material ambiguity, and reread final edited files before concluding. `agents/rose.md` supplies the compact routing reference. Umbrella fixture rows that previously named the retired skill are migrated to this canonical owner; they do not create a compatibility alias.

[KNOWN] Active source-register, fixture, and aggregate-evidence rows use the retained Command, Skill, and MemPalace provider owners only. Retirement metadata is limited to explicit migration inventory and does not create routing, ownership, or executable test evidence.
