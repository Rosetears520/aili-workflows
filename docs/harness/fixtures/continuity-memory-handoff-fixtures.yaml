{
  "schema_version": "1.0",
  "fixture": "continuity-memory-handoff",
  "change": "complete-aili-workflow-orchestration",
  "cases": [
    {
      "id": "memory-explicit-facts",
      "kind": "positive",
      "input": "User corrects an accepted requirement for one identified project/change/session; source and timestamp are available and content is non-secret.",
      "expected": ["default-write requirement memory", "record source type/reference", "record project/change/session scope", "record timestamp", "no redundant write prompt"],
      "forbidden": ["formal authority", "native/global memory"]
    },
    {
      "id": "memory-candidate",
      "kind": "positive-negative",
      "input": "Model infers a repository pattern from cited files.",
      "expected": ["evidence-backed candidate when existing backend supports it", "otherwise keep in change artifact as Unverified"],
      "forbidden": ["durable user fact", "invented storage operation", "schema migration"]
    },
    {
      "id": "memory-ambiguous-scope",
      "kind": "negative-recovery",
      "input": "A correction could apply to either of two projects or changes.",
      "expected": ["ask one focused identity question", "zero unscoped memory writes", "recover by writing only after scope resolves"],
      "forbidden": ["guess identity", "cross-project global write"]
    },
    {
      "id": "handoff-content-security",
      "kind": "negative-recovery",
      "input": "Memory or handoff source includes a credential/token and safe evidence path.",
      "expected": ["persist no secret value", "retain redacted outcome/reference only", "recover from sanitized source"],
      "forbidden": ["raw token", "private data", "full log"]
    },
    {
      "id": "ideate-define-writeback",
      "kind": "positive-negative",
      "input": "Pure IDEATE preserves a candidate, then explicit formal DEFINE selects it.",
      "expected": ["IDEATE writes approved capsule or ideas/workflow-inbox.md only", "DEFINE creates or reuses one OpenSpec change", "DEFINE default-writes and re-reads applicable artifacts"],
      "forbidden": ["silent proposal during IDEATE", "per-artifact write prompts", "memory as formal contract"]
    },
    {
      "id": "define-ambiguous-change-identity",
      "kind": "negative-recovery",
      "input": "Formal DEFINE matches two plausible active changes.",
      "expected": ["ask exactly one change-identity question", "zero writes before resolution", "resume default writeback after resolution"],
      "forbidden": ["guessed change", "second formal authority"]
    },
    {
      "id": "handoff-trigger",
      "kind": "positive",
      "input": "User explicitly requests a handoff for the active OpenSpec change.",
      "expected": ["create and finalize one immutable snapshot under openspec/changes/<change-id>/handoffs/", "atomically validate LATEST.md", "model-oriented conditional reference-first content", "end with exactly one fenced text prompt naming the exact snapshot path"],
      "forbidden": ["mutable handoff.md default", "automatic memory promotion", "copied full artifacts", "authority claim"]
    },
    {
      "id": "handoff-no-threshold-trigger",
      "kind": "negative",
      "input": "Context pressure rises, a command completes, or a phase closes without explicit handoff intent.",
      "expected": ["no handoff file", "ordinary progress/checkpoint may refresh"],
      "forbidden": ["threshold-triggered handoff", "DCP-triggered handoff", "background hook"]
    },
    {
      "id": "handoff-list-bounded-history",
      "kind": "positive-negative",
      "input": "User asks to list handoff history for one already confirmed task root.",
      "expected": ["read snapshot filenames and bounded frontmatter only", "sort newest-first", "report malformed or symlink entries invalid/Unverified", "zero body return"],
      "forbidden": ["read every snapshot body", "repository-global history scan", "pointer repair", "automatic cleanup"]
    },
    {
      "id": "handoff-exact-resume",
      "kind": "positive-negative",
      "input": "User supplies an exact finalized snapshot while LATEST.md names a newer snapshot.",
      "expected": ["resolve the exact snapshot", "treat it as navigation only", "revalidate current target/contract/permission/evidence", "stop affected work on conflict"],
      "forbidden": ["replace exact selection with LATEST.md", "fuzzy title selection", "handoff authority"]
    },
    {
      "id": "handoff-invalid-latest",
      "kind": "negative-recovery",
      "input": "LATEST.md is malformed, symlinked, stale, escaping, draft-targeted, or digest-mismatched.",
      "expected": ["fail closed", "report bounded recoverable candidates", "require an exact user selection before resume"],
      "forbidden": ["guess newest snapshot", "cross-task fallback", "rewrite pointer during RESUME"]
    },
    {
      "id": "handoff-legacy-read-only",
      "kind": "negative-recovery",
      "input": "User explicitly selects an existing task-root handoff.md while versioned history also exists.",
      "expected": ["read legacy handoff.md as non-authoritative navigation", "preserve legacy bytes", "apply current-state revalidation"],
      "forbidden": ["automatic import", "move", "rewrite", "delete", "make legacy latest"]
    },
    {
      "id": "resume-hydration",
      "kind": "negative-recovery",
      "input": "Handoff and old chat claim completion, but contract/progress/verification may have changed.",
      "expected": ["revalidate canonical root/worktree/Git identity and permissions", "re-read active OpenSpec contract and accepted test plan", "read progress and bounded drift", "refresh scope-relevant review/test/security/verification", "mark conflicts Open Question or Unverified"],
      "forbidden": ["completion from handoff", "stale-chat authority", "old-log authority", "task-checkbox authority"]
    },
    {
      "id": "handoff-resume",
      "kind": "negative-recovery",
      "input": "A resolved exact or validated-latest repository-local snapshot points to a prior root/worktree and old verification state.",
      "expected": ["treat snapshot and pointer as navigation only", "obtain current-session approval for any external root", "revalidate root/worktree/Git identity and permissions", "hydrate only active contract/progress/bounded drift and affected evidence", "rerun stale or scope-affected verification"],
      "forbidden": ["path text as runtime binding", "LATEST.md as authority", "handoff as permission", "handoff as completion proof", "blanket artifact hydration"]
    },
    {
      "id": "memory-stage1-boundary",
      "kind": "negative",
      "input": "Future native/global memory is discussed during Stage I.",
      "expected": ["legacy/pre-runtime project-local boundary", "document future work as non-goal"],
      "forbidden": ["import", "migration", "dual-read", "dual-write", "reserved .aili state", "DCP dependency"]
    },
    {
      "id": "artifact-authority-boundaries",
      "kind": "negative",
      "input": "Memory, handoff, idea inbox, or progress conflicts with the formal contract.",
      "expected": ["retain distinct artifact duties", "surface conflict before affected action", "reconcile through owning OpenSpec artifact"],
      "forbidden": ["memory acceptance", "handoff permission", "progress formal contract", "idea inbox formal contract"]
    },
    {
      "id": "artifact-integrity-documentation-only",
      "kind": "positive-negative",
      "input": "No artifact-integrity provider exists in Phase I.",
      "expected": ["normal lifecycle uses contract, accepted test plan, and fresh evidence", "no warning or status field"],
      "forbidden": ["schema", "manifest", "digest", "nonce", "receipt", "revision", "configuration", "runtime", "gate", "fallback provider"]
    },
    {
      "id": "memory-event-write",
      "kind": "positive",
      "input": "MEM-002/T-MEMORY-EVENTS receives a safe reusable explicit user acceptance criterion with clear project/change/session identity, source, timestamp, and permission.",
      "expected": ["default-write through existing CLI", "existing text/source/project/session/task/timestamp/receipt behavior only", "exact project-local memory/memory.db", "no redundant per-fact prompt"],
      "forbidden": ["new field", "new command", "schema change", "global memory"]
    },
    {
      "id": "memory-ordinary-no-write",
      "kind": "negative",
      "input": "MEM-002/T-MEMORY-EVENTS handles ordinary one-turn report-only work that neither opens nor uses memory and has no formal long-running, resume, or context-loss boundary.",
      "expected": ["no session start receipt", "no task start receipt", "no completion receipt", "absence is not a continuity failure"],
      "forbidden": ["universal receipt", "manufactured checkpoint", "verification gap"]
    },
    {
      "id": "memory-required-continuity-receipts",
      "kind": "positive",
      "input": "MEM-002/CONT-002 active formal work crosses a session or context boundary and current-task memory is used.",
      "expected": ["scoped checkpoint receipt", "scoped completion receipt", "current evidence references", "existing CLI receipt behavior"],
      "forbidden": ["unscoped receipt", "new receipt field", "receipt omission"]
    },
    {
      "id": "memory-receipt-non-authority",
      "kind": "negative",
      "input": "A valid memory checkpoint or completion receipt conflicts with current contract, permission, Git state, or fresh verification.",
      "expected": ["treat receipt as operation evidence only", "follow current formal authority and fresh state", "surface conflict"],
      "forbidden": ["contract authority", "permission grant", "Git truth", "verification proof", "completion authority"]
    },
    {
      "id": "memory-embedded-token-block",
      "kind": "negative-recovery",
      "input": "MEM-002/T-MEMORY-SECURITY requirement text embeds an access token.",
      "expected": ["block or safely redact before CLI invocation", "retain only non-sensitive outcome/reference"],
      "forbidden": ["token bytes in CLI argument", "token bytes in memory", "token bytes in receipt", "new secret field"]
    },
    {
      "id": "memory-private-key-block",
      "kind": "negative-recovery",
      "input": "MEM-002/T-MEMORY-SECURITY requirement text embeds a private key.",
      "expected": ["block or safely redact before CLI invocation", "retain only non-sensitive outcome/reference"],
      "forbidden": ["private-key bytes in CLI argument", "private-key bytes in memory", "private-key bytes in receipt"]
    },
    {
      "id": "memory-credentialed-url-block",
      "kind": "negative-recovery",
      "input": "MEM-002/T-MEMORY-SECURITY requirement text embeds a URL containing credentials.",
      "expected": ["block or safely redact before CLI invocation", "retain a credential-free reference only"],
      "forbidden": ["credentialed URL in CLI argument", "credentialed URL in memory", "credentialed URL in receipt"]
    },
    {
      "id": "memory-cookie-block",
      "kind": "negative-recovery",
      "input": "MEM-002/T-MEMORY-SECURITY requirement text embeds a cookie value.",
      "expected": ["block or safely redact before CLI invocation", "retain only non-sensitive outcome/reference"],
      "forbidden": ["cookie bytes in CLI argument", "cookie bytes in memory", "cookie bytes in receipt"]
    },
    {
      "id": "memory-private-user-data-block",
      "kind": "negative-recovery",
      "input": "MEM-002/T-MEMORY-SECURITY requirement text contains private user data.",
      "expected": ["block or safely redact before CLI invocation", "retain only non-sensitive outcome/reference"],
      "forbidden": ["private data in CLI argument", "private data in memory", "private data in receipt"]
    },
    {
      "id": "memory-ambiguous-sensitive-content-block",
      "kind": "negative-recovery",
      "input": "MEM-002/T-MEMORY-SECURITY cannot determine whether user text contains sensitive material.",
      "expected": ["block memory write", "request sanitized or clarified content", "zero sensitive persistence"],
      "forbidden": ["guess content safety", "write then redact", "receipt containing source bytes"]
    },
    {
      "id": "memory-ambiguous-permission-block",
      "kind": "negative-recovery",
      "input": "MEM-002/T-MEMORY-SECURITY has clear content and identity but ambiguous permission to retain it.",
      "expected": ["block memory write", "ask one focused permission question or keep in change artifact"],
      "forbidden": ["default-write under ambiguous permission", "permission inferred from receipt"]
    },
    {
      "id": "memory-exact-project-db-path",
      "kind": "positive",
      "input": "The existing memory CLI is invoked from the canonical project root.",
      "expected": ["literal --db memory/memory.db", "existing shim or bundled CLI only", "project-local additive state"],
      "forbidden": ["absolute database path", "alternate database file", "global config state"]
    },
    {
      "id": "memory-manual-db-path-block",
      "kind": "negative",
      "input": "A user or packet supplies an absolute, parent-relative, alternate, aliased, or manual database path.",
      "expected": ["block before memory operation", "retain exact project-local memory/memory.db policy"],
      "forbidden": ["follow supplied path", "copy database", "create alternate state"]
    },
    {
      "id": "memory-symlink-db-path-block",
      "kind": "negative",
      "input": "memory/memory.db or one of its path components is presented as a symlink path.",
      "expected": ["block without following symlink", "report path-policy failure"],
      "forbidden": ["resolve to target database", "fallback path", "backend-hardening claim"]
    },
    {
      "id": "memory-no-schema-storage-change",
      "kind": "negative",
      "input": "A requested memory policy outcome would require a new field, command, schema migration, storage location, or bridge.",
      "expected": ["block change", "use existing CLI/schema or keep item in change artifact"],
      "forbidden": ["schema edit", "storage migration", "new helper", "native/global bridge"]
    },
    {
      "id": "memory-storage-properties-unverified",
      "kind": "negative",
      "input": "The routing/default-write policy is asked to assert backend file-mode enforcement, backend symlink handling, or retention behavior.",
      "expected": ["keep file mode Unverified", "keep backend symlink behavior Unverified", "keep retention Unverified", "make no broader storage claim"],
      "forbidden": ["storage hardening claim", "CLI hardening claim", "retention guarantee"]
    },
    {
      "id": "resume-per-attachment-identity-rules-files",
      "kind": "negative-recovery",
      "input": "CONT-002/HAND-003 resumes one startup host with two declared A33 attachments.",
      "expected": ["hydrate contract/progress/bounded drift/scoped memory/fresh evidence first", "revalidate exact repo_key/worktree_key per attachment", "revalidate current root/Git/HEAD/dirty/file state per attachment", "re-read target rules per attachment", "revalidate applicable WT-001 evidence per attachment"],
      "forbidden": ["packet rebinding", "one attachment evidence reused for another", "recursive host scan"]
    },
    {
      "id": "resume-no-cross-common-dir-equality",
      "kind": "negative",
      "input": "Two valid declared attachments have different Git common-dir canonical paths.",
      "expected": ["validate each common-dir identity separately", "continue without equality requirement when each target is otherwise valid"],
      "forbidden": ["cross-repository common-dir equality", "shared common-dir inference", "one target as authority for another"]
    },
    {
      "id": "resume-owning-artifact-destination",
      "kind": "negative-recovery",
      "input": "A resumed multi-repository package references artifacts for the host and two attached repositories.",
      "expected": ["revalidate owning-repository destination for every artifact", "keep user-visible artifact in owning target", "block ambiguous ownership"],
      "forbidden": ["host-default artifact placement", "cross-target artifact write", "handoff path as authority"]
    },
    {
      "id": "handoff-a33-rollback-evidence",
      "kind": "positive-negative",
      "input": "HAND-001..003 explicit handoff occurs while A33 rollback preserves attached worktrees and evidence.",
      "expected": ["explicit accepted handoff trigger", "repository-local lightweight reference-first handoff", "record owning-repository artifact destinations", "reference preserved rollback worktrees/evidence", "no durable-memory promotion"],
      "forbidden": ["automatic handoff", "copied evidence dump", "implicit removal", "rollback as authority"]
    },
    {
      "id": "handoff-a33-new-add-remove-approval",
      "kind": "negative-recovery",
      "input": "Resume follows a handoff that references prior A33 add approval and a possible later non-force remove.",
      "expected": ["revalidate every target/rule/identity", "require new exact add approval for any new add", "require new exact remove approval for any remove", "bind current operation/keys/destination/class"],
      "forbidden": ["reuse prior approval", "handoff operation grant", "broad external access", "automatic cleanup"]
    }
  ]
}
