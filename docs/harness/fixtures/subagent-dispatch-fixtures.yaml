{
  "fixture": "subagent-dispatch",
  "required_markers": ["trace_id", "work_package_type", "artifact_target", "coverage_expectation", "known_exclusions", "evidence_anchors", "package-queue", "implementer", "complete, appropriately scoped, verified", "not artificially tiny", "most relevant focused verification", "classify dirty paths", "approval-gated cleanup", "cleanup package", "parallelism analysis", "join completeness", "package/lane preservation", "no-parallel reason"],
  "packet_cases": [
    {"id": "packet-docs", "trace_id": "TRACE-DOCS-001", "work_package_type": "implementation", "artifact_target": "docs/harness", "coverage_expectation": "tasks 2.1-2.7", "known_exclusions": ["commands", "skills"]},
    {"id": "packet-build-neutral-package", "trace_id": "TRACE-BUILD-PACKAGE-001", "work_package_type": "implementation", "agent": "implementer", "artifact_target": "package-queue", "coverage_expectation": "next dependency-ready BUILD package", "allowed_scope": "single synthesized package", "forbidden_scope": ["out-of-scope packages", "high-risk gates without approval", "package-local mandatory quality gate"], "edit_permission": "package-scoped", "commit_allowance": "active-contract", "known_exclusions": ["out-of-scope packages", "high-risk gates without approval"], "parallelism_analysis": ["parallelism analysis", "no-parallel reason", "join completeness", "package/lane preservation"], "join_contract": ["join points", "blockers", "expected evidence"], "required_evidence": ["evidence_anchors", "changed_files", "scope_boundary", "lightweight savepoint"], "implementation_objective": ["complete, appropriately scoped, verified", "complete task-scoped", "not artificially tiny"], "verification_order": ["most relevant focused verification is optional feedback"], "review_repair": null, "cleanup_gate": ["branch/status", "classify dirty paths", "task-scoped", "unrelated/pre-existing", "generated/ignored", "scratch", "unknown", "cleanup proposal", "approval-gated cleanup", "push", "destructive clean/reset", "branch deletion", "worktree removal", "OpenSpec archive", "stashing unrelated changes", "deleting user-visible artifacts", "task-scoped verified commits", "cleanup package"]},
    {"id": "packet-shared-edit-conflict", "trace_id": "TRACE-SHARED-CONFLICT-001", "work_package_type": "implementation", "artifact_target": "shared core file", "allowed_scope": "shared scaffold first", "forbidden_scope": ["parallel edits to same file"], "edit_permission": "sequential-or-single-editor", "parallelism_analysis": ["shared scaffold/source-of-truth work", "no-parallel reason", "shared mutable state", "later fan-out after join"], "join_contract": ["join points", "blockers", "expected evidence"], "required_evidence": ["ownership boundaries", "verification", "scope_boundary"]}
  ],
  "result_cases": [
    {"id": "result-pass", "status": "pass", "confidence": "high", "evidence_anchors": ["docs/harness/aili-harness-contract.md"], "coverage": "package scope", "skipped_work": ["forbidden files"], "unknowns": [], "harness_failure_signals": []},
    {"id": "final-review-subagent-non-nesting", "status": "pass", "package": "P12", "subagent_calls": 0, "returns_to": "ROSE", "majority_vote": false, "overlay": {"edit": "deny", "task": "deny"}},
    {"id": "final-review-test-engineer-edit-denied", "status": "deny-before-mutation", "package": "P12", "agent": "test-engineer", "attempt": "edit focused test", "overlay": {"edit": "deny", "task": "deny"}}
  ]
}
