BUILD progress ledger: add-shared-agents-skills-qa-traceability

Current goal
- Implement the approved OpenSpec change for `.agents/skills` canonical skill source migration, OpenCode-native preservation, manifest source-of-truth, QA/testing agents+skills, Code-Spec traceability, AGENTS freshness, CodeGraph readiness, and selected DeerFlow skill absorption.

Confirmed decisions
- User approved production implementation edits on `main` for this BUILD.
- User approved high-risk source migration: canonical `skills/*` may move to `.agents/skills/*`; old `skills/` should be removed or made generated/non-authoritative as needed.
- Preserve OpenCode-native install outputs.
- Preserve `.playwright-mcp/` untouched.
- Preserve `openspec/` ignored policy.
- Upstream copied or closely paraphrased content requires source/license/provenance/notice and minimal AILI/OpenCode adaptation.

Package queue
1. Manifest/source scaffold: manifest schema/data, TypeScript validators, package inclusion, installer/doctor source-target model.
2. Skill source migration: move canonical skills to `.agents/skills`, update Bash/TS installer and package behavior, preserve OpenCode-native outputs.
3. Docs/AGENTS freshness: update templates, generated AGENTS, README/setup docs/checks.
4. QA agents+skills: add specialized testing agents and matching skills, route/review pipeline updates.
5. Traceability/lifecycle: requirement→task→file→verification mapping and SHIP spec coverage guidance.
6. CodeGraph readiness: doctor/setup/tests for optional uninitialized status.
7. DeerFlow absorption/provenance: selected skill/pattern additions with provenance.
8. Verification/review closeout.

Parallelism / sequencing
- Package 1 is shared scaffold and runs first.
- Packages 3, 5, and 6 can mostly parallelize after package 1 if file ownership is separated.
- Packages 4 and 7 depend on settled `.agents/skills` source model and manifest handling.
- Package 8 is a serial join.

Current package
- SHIP review/repair/verification complete; ready for user decision on commit/staging/archive.

Completed packages
- Package 1: Manifest/source scaffold.
  - Changed `manifests/rose-aili.components.json`, `src/manifest.ts`, `src/installer.ts`, `src/doctor.ts`, `package.json`, and `tests/rose-aili.test.mjs`.
  - Added component fields `sourcePath`, `sourceFallbackPaths`, and `installTargets`.
  - Declared planned `.agents/skills/<name>` canonical/shared skill source and preserved OpenCode-native `skills/<name>` target for existing skills.
  - Updated manifest validation and doctor target checks.
  - Verification reported by implementer: `npm run typecheck` passed; `npm test` passed with 59 tests, 58 passed, 1 skipped.
  - ROSE diff inspection completed for the changed files; no `.playwright-mcp/` changes observed.
- Package 2: Skill source migration.
  - Moved tracked repository-managed `skills/*` to `.agents/skills/*`.
  - Updated `manifests/rose-aili.components.json`, `scripts/install_opencode.sh`, `src/manifest.ts`, `src/installer.ts`, `src/doctor.ts`, `package.json`, `tests/rose-aili.test.mjs`, `scripts/harness_fixture_check.py`, `scripts/delegation_protocols_check.py`, `workflow.components.yaml`, and `docs/harness/fixtures/agents-template-fixtures.yaml`.
  - OpenCode-native install target remains `skills/<name>`; `.agents/skills` is now canonical source.
  - Verification reported by implementer: `npm run typecheck`, `npm test`, `bash -n scripts/install_opencode.sh`, `npm pack --dry-run`, `python scripts/harness_fixture_check.py`, `python scripts/delegation_protocols_check.py`, `python scripts/agents_md.py check --project .`, and `python -m py_compile scripts/harness_fixture_check.py scripts/agents_md.py scripts/delegation_protocols_check.py` all passed.
  - ROSE status/stat inspection observed tracked renames and no `.playwright-mcp/` changes.
- Package 3: Docs/AGENTS freshness.
  - Updated `AGENTS.md`, `README.md`, and `docs/opencode-setup.md` for current TypeScript/Node facts and canonical `.agents/skills` source while preserving OpenCode-native installed target wording.
  - Repair lane updated `templates/AGENTS.md` so root `AGENTS.md` remains generated from the template.
  - Verification reported: `python scripts/agents_md.py check --project .`, exact template/root comparison, `python scripts/harness_fixture_check.py`, and `python -m py_compile scripts/agents_md.py` passed where applicable.
- Package 5: Traceability/lifecycle guidance.
  - Updated `.agents/skills/aili-delivery-flow/**`, `.agents/skills/test-document-generator/**`, `.agents/skills/review-pipeline/SKILL.md`, `.agents/skills/verification-before-completion/SKILL.md`, and harness fixture/check coverage.
  - Added requirement/decision/risk → package/task → file/artifact → verification/evidence mapping and spec coverage gating.
  - Verification reported: Python compile, harness fixture check, and `openspec validate add-shared-agents-skills-qa-traceability --strict` passed.
- Package 6: CodeGraph doctor readiness.
  - Updated `src/doctor.ts` and `tests/rose-aili.test.mjs` to separate OpenCode MCP status from optional project-local CodeGraph index readiness.
  - `.codegraph/` remains untouched; doctor reports absent index as optional not-initialized with explicit follow-up commands.
  - Verification reported: `npm run typecheck`, targeted doctor tests, and `npm test` passed.
- Package 4: QA/testing agents and matching skills.
  - Added agents: `test-coverage-reviewer`, `pr-test-analyzer`, `ai-regression-scout`, `silent-failure-reviewer`, `browser-qa-runner`, `e2e-artifact-runner`.
  - Added matching skills under `.agents/skills/`: `coverage-review`, `pr-test-analysis`, `ai-regression-scout`, `silent-failure-hunting`, `browser-qa`, `e2e-artifact-handling`.
  - Updated `agents/rose.md`, `.agents/skills/review-pipeline/SKILL.md`, manifest, tests, README, and setup docs.
  - Verification reported: `npm run typecheck`, `npm test` (rerun with longer timeout), `python scripts/harness_fixture_check.py`, `python scripts/agents_md.py check --project .`, and `npm pack --dry-run` passed.
- Package 7: DeerFlow selected skill/pattern absorption.
  - Added clean-room skills: `academic-paper-review`, `systematic-literature-review`, `newsletter-generation`, `consulting-analysis`, `data-analysis`, and `chart-visualization`.
  - Updated `skill-authoring-and-validation`, `mature-project-pattern-research`, `github-evidence-triage`, `documentation-and-adrs`, `frontend-ui-engineering`, and `browser-testing-with-devtools` for selected DeerFlow-inspired trigger/progressive-disclosure/research/report/data-authenticity/UI-audit patterns.
  - Updated manifest, README provenance/catalog notes, and tests.
  - Verification reported: `npm run typecheck`, `npm test` (rerun with longer timeout), `python scripts/harness_fixture_check.py`, `python scripts/agents_md.py check --project .`, and `npm pack --dry-run` passed.
  - Provenance note: implementer reports clean-room AILI/OpenCode adaptations; no upstream skill text, runtime assumptions, provider/media/deploy skills, or DeerFlow branding copied.

Evidence anchors
- `proposal.md`, `design.md`, `tasks.md`, `interview.md`, and `test-plan.md` in this change.
- Local scout identified manifest/installer targets: `manifests/rose-aili.components.json`, `src/manifest.ts`, `src/installer.ts`, `src/doctor.ts`, `scripts/install_opencode.sh`, `package.json`, `tests/rose-aili.test.mjs`.
- External docs lane confirmed OpenCode supports `.agents/skills` and Agent Skills recommends `.agents/skills` as a cross-client convention.

Verification plan
- Package-level: targeted tests for changed source.
- Final: `openspec validate add-shared-agents-skills-qa-traceability --strict`, `npm run typecheck`, `npm test`, `npm run build` when TS/package changes, `bash -n scripts/install_opencode.sh` when Bash changes, Python compile/checks if scripts change, `python scripts/harness_fixture_check.py`, `python scripts/agents_md.py check --project .`, `npm pack --dry-run`, review/test/security lanes, final diff inspection.

Blockers / unverified
- No release-blocking blockers remain after SHIP repair and fresh verification.
- CodeGraph index remains unavailable until explicit project-local init; fallback read/search evidence is used.
- Live OpenCode runtime routing was structurally verified through package/install/manifest tests but not exercised end-to-end in a real OpenCode session.
- `.playwright-mcp/` remains intentionally uninspected and untouched.

Next action
- Await user decision for commit/staging/OpenSpec archive or follow-up cleanup. Do not push/archive/commit/stash/clean without explicit approval.

Final verification evidence
- BUILD-final run: `openspec validate add-shared-agents-skills-qa-traceability --strict`, `npm run typecheck`, `npm run build`, `npm test` (66 passed / 1 skipped), Bash syntax, Python compile/checks, harness, AGENTS, delegation, and `npm pack --dry-run` all passed before SHIP audit.
- SHIP repair run after installer/doctor/docs/artifact fixes:
  - `openspec validate add-shared-agents-skills-qa-traceability --strict`: PASS.
  - `npm run typecheck`: PASS.
  - `npm run build`: PASS.
  - `npm test`: PASS, 70 passed / 1 skipped.
  - `bash -n scripts/install_opencode.sh`: PASS.
  - `python -m py_compile scripts/harness_fixture_check.py scripts/agents_md.py scripts/delegation_protocols_check.py`: PASS.
  - `python scripts/harness_fixture_check.py`: PASS.
  - `python scripts/agents_md.py check --project .`: PASS.
  - `python scripts/delegation_protocols_check.py`: PASS.
  - `npm pack --dry-run`: PASS; package includes `.agents/skills`, new agents, manifest, docs, and excludes old canonical `skills/`, `.codegraph/`, and `.playwright-mcp/` per tests/package output.
  - `npm audit --omit=dev`: PASS, 0 vulnerabilities.

Review evidence
- Initial code/test/security review found blockers/gaps: generic AGENTS template source, stale harness `skills/` docs, `.playwright-mcp/` hygiene, selective symlink coverage, QA routing checks, CodeGraph initialized coverage, package tar coverage, OPENCODE_HOME traversal, and DeerFlow provenance table.
- Repair package addressed those findings and reran full verification.
- Follow-up code review: PASS.
- Follow-up test review: PASS, with non-blocking Unverified note that live OpenCode runtime routing is structural/contract-tested rather than end-to-end executed.
- Follow-up security review: PASS, with non-blocking Unverified note that CodeGraph remained uninitialized and symlink-to-unsafe-root behavior was not runtime-tested beyond the reviewed tmp traversal class.
- SHIP release-blocker audit initially returned NEEDS_FIXES for Bash manifest drift validation, doctor source-readiness reporting, stale `.agents/skills` wording, README provenance, stale OpenSpec artifacts, and missing closeout.
- SHIP code repair lane: PASS; fixed direct Bash manifest drift checks and doctor source-readiness reporting/tests.
- SHIP docs/artifact repair lane: PASS; fixed stale source-path wording, README DeerFlow provenance, and OpenSpec context/test-plan/proposal consistency.
- ROSE fresh verification after repairs: PASS.

Dirty-state classification
- Task-scoped tracked changes: `.agents/skills` migration, manifest/installer/doctor/tests/docs/AGENTS/README/harness scripts, new QA agents, new clean-room skills, `.gitignore` hygiene.
- Task-scoped untracked files: new QA/DeerFlow-inspired skill directories under `.agents/skills/*` and new agent markdown files under `agents/`.
- Ignored/pre-existing residue: `.playwright-mcp/` remains local and untouched; now ignored.
- Generated ignored outputs: `dist/` rebuilt by verification and remains ignored.
- No commit, push, OpenSpec archive, destructive cleanup, or stash performed.

Remaining Unverified / residual risks
- CodeGraph project index is not initialized; graph-backed evidence remains unavailable.
- Live OpenCode runtime discovery/routing was not exercised end-to-end; structural tests and packaging checks passed.
- Existing `.playwright-mcp/` contents were intentionally not inspected or deleted.
- Previous-release baseline comparison remains Unverified because no baseline artifact was provided.
